{
  "name": "travel-assistant-openapi-conformance",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only OpenAPI and runtime contract classification for Travel Assistant.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run -s build && node dist/cli.js",
    "test": "npm run -s build && node --test dist/report.test.js"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
